@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/RuleForgeTargets.cmake")

check_required_components(RuleForge)

# Provide convenient alias
if(NOT TARGET RuleForge::RuleForge)
    add_library(RuleForge::RuleForge ALIAS RuleForge::rule_forge)
endif()
